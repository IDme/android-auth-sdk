<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="ID.me SDK Demo"
        android:supportsRtl="true"
        android:theme="@style/Theme.IDmeAuthDemo">

        <activity
            android:name="com.idme.auth.demo.IDmeAuthDemoActivity"
            android:exported="true"
            android:theme="@style/Theme.IDmeAuthDemo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SDK redirect handler for the demo app -->
        <activity
            android:name="com.idme.auth.auth.IDmeRedirectActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="${idmeRedirectScheme}"
                    android:host="idme"
                    android:path="/callback" />
            </intent-filter>
        </activity>

    </application>

</manifest>
